import React from 'react';

function CallRecorder() {
  return (
    <div>
      <h2>Record the Prank Call</h2>
      <p>In the Calling section, enable the "Record Call" toggle before placing the call. Once the prank call ends, the recording will be available in the Recordings section. Listen to the recordings and delete them when done.</p>
    </div>
  );
}

export default CallRecorder;